<Window x:Class="CopilotSessionManager.Views.SessionTokensWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Token usage"
        Height="430"
        Width="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True">
    <Window.Resources>
        <!-- Themed DataGrid chrome so per-model column headers + cells use the active theme palette
             (otherwise WPF defaults render column headers in system white, unreadable on dark themes). -->
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SurfaceHoverBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type DataGridRow}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Session header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock x:Name="HeaderSessionText"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       TextTrimming="CharacterEllipsis" />
            <TextBlock x:Name="HeaderSubText"
                       FontSize="12"
                       Margin="0,4,0,0"
                       Foreground="{DynamicResource TextMutedBrush}"
                       TextWrapping="Wrap" />
        </StackPanel>

        <!-- Provenance banner (live snapshot vs final shutdown) -->
        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceAltBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10,6"
                Margin="0,0,0,10">
            <TextBlock x:Name="ProvenanceText"
                       FontSize="12"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       TextWrapping="Wrap" />
        </Border>

        <!-- Per-model token breakdown -->
        <DataGrid Grid.Row="2"
                  x:Name="UsageGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0"
                  Background="{DynamicResource SurfaceBrush}"
                  Foreground="{DynamicResource TextPrimaryBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                  RowBackground="{DynamicResource SurfaceBrush}"
                  AlternatingRowBackground="{DynamicResource SurfaceAltBrush}"
                  FontSize="12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Model" Width="*" Binding="{Binding Model}" />
                <DataGridTextColumn Header="Requests" Width="80" Binding="{Binding RequestsDisplay}" />
                <DataGridTextColumn Header="Input" Width="90" Binding="{Binding InputDisplay}" />
                <DataGridTextColumn Header="Output" Width="90" Binding="{Binding OutputDisplay}" />
                <DataGridTextColumn Header="Cache read" Width="100" Binding="{Binding CacheReadDisplay}" />
                <DataGridTextColumn Header="Cache write" Width="100" Binding="{Binding CacheWriteDisplay}" />
                <DataGridTextColumn Header="Reasoning" Width="90" Binding="{Binding ReasoningDisplay}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals strip -->
        <Border Grid.Row="3"
                Margin="0,8,0,0"
                Background="{DynamicResource OverlayBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10,8">
            <TextBlock x:Name="TotalsText"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       TextWrapping="Wrap" />
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button x:Name="CopyButton"
                    Content="Copy summary"
                    Padding="14,6"
                    Margin="0,0,8,0"
                    Background="{DynamicResource SurfaceHoverBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    Click="OnCopyClicked" />
            <Button x:Name="RefreshButton"
                    Content="Refresh"
                    Padding="14,6"
                    Margin="0,0,8,0"
                    Background="{DynamicResource SurfaceHoverBrush}"
                    Foreground="{DynamicResource TextPrimaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    Click="OnRefreshClicked" />
            <Button x:Name="CloseButton"
                    Content="Close"
                    Padding="14,6"
                    Background="{DynamicResource PrimaryActionBrush}"
                    Foreground="{DynamicResource TextOnAccentBrush}"
                    BorderBrush="{DynamicResource PrimaryActionBorderBrush}"
                    IsCancel="True"
                    Click="OnCloseClicked" />
        </StackPanel>
    </Grid>
</Window>
